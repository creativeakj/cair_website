// Publications is content-complete but temporarily hidden from the public
// site (nav, homepage, footer) while its rollout is paused. The admin panel
// keeps managing publications as normal, and /publications and its detail
// pages still exist - they're just not linked from anywhere prominent.
// Flip this back to true to restore all three surfaces at once.
export const SHOW_PUBLICATIONS = false;
